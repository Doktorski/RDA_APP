@model IEnumerable<RDA_APP.Models.Narudzbenica>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Broj_narudzbenice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Datum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kompanija.Naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zaposleni.Ime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Broj_narudzbenice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Datum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kompanija.Naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zaposleni.Ime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Narudzbenica_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Narudzbenica_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Narudzbenica_ID })
        </td>
    </tr>
}

</table>
